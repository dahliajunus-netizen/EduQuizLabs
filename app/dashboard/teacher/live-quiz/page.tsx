'use client';

import { useEffect, useState } from 'react';
import { useRouter } from 'next/navigation';
import { Button } from '@/components/ui/button';
import { Card, CardContent, CardHeader, CardTitle } from '@/components/ui/card';
import { Input } from '@/components/ui/input';
import { ArrowLeft, Play, Plus, Users, Trophy, Clock, MonitorPlay, Copy, Check, Sparkles, Radio, ChevronRight } from 'lucide-react';

const url = process.env.NEXT_PUBLIC_SUPABASE_URL!;
const key = process.env.NEXT_PUBLIC_SUPABASE_ANON_KEY!;
const headers = { apikey: key, Authorization: `Bearer ${key}`, 'Content-Type': 'application/json' };
type Q = { id: string; question_order: number; question: string; option_a: string; option_b: string; option_c: string; option_d: string; correct_answer: string; time_limit_seconds: number };
type Quiz = { id: string; title: string; class_code: string; game_code: string | null; status: string; current_question: number; is_template: boolean };
type Player = { id: string; nickname: string; score: number; total_response_time_ms: number; correct_answers: number };
type TeacherClass = { id?: string; class_name: string; school_name?: string | null; code: string; teacher_id?: string | null };
const answerColors = [
  { letter: 'A', label: '▲', className: 'bg-red-500 hover:bg-red-600' },
  { letter: 'B', label: '◆', className: 'bg-blue-500 hover:bg-blue-600' },
  { letter: 'C', label: '●', className: 'bg-yellow-500 hover:bg-yellow-600' },
  { letter: 'D', label: '■', className: 'bg-green-500 hover:bg-green-600' },
];
function teacherId() { try { const u = JSON.parse(localStorage.getItem('current_user') || '{}'); return String(u.id ?? u.user_id ?? u.uid ?? '') || null; } catch { return null; } }
function code() { return Math.random().toString(36).slice(2, 8).toUpperCase(); }

async function api(path: string, opts: RequestInit = {}) {
  const r = await fetch(`${url}/rest/v1/${path}`, { ...opts, headers: { ...headers, ...(opts.headers || {}) }, cache: 'no-store' });
  const text = await r.text();
  if (!r.ok) throw new Error(text || `Request failed (${r.status})`);
  if (!text.trim()) return null;
  try { return JSON.parse(text); } catch { throw new Error(`Supabase returned an invalid response (${r.status}).`); }
}

export default function TeacherLiveQuiz() {
  const router = useRouter();
  const [templates, setTemplates] = useState<Quiz[]>([]), [selected, setSelected] = useState<Quiz | null>(null), [quiz, setQuiz] = useState<Quiz | null>(null), [questions, setQuestions] = useState<Q[]>([]), [players, setPlayers] = useState<Player[]>([]), [classes, setClasses] = useState<TeacherClass[]>([]), [title, setTitle] = useState('My Live Quiz'), [classCode, setClassCode] = useState(''), [q, setQ] = useState(''), [a, setA] = useState(''), [b, setB] = useState(''), [c, setC] = useState(''), [d, setD] = useState(''), [correct, setCorrect] = useState('A'), [loading, setLoading] = useState(false), [error, setError] = useState(''), [remaining, setRemaining] = useState(30), [copied, setCopied] = useState(false), [showAnswers, setShowAnswers] = useState(false);
  const current = quiz?.current_question ?? -1; const currentQ = questions[current];
  const loadTemplates = async () => { try { const id = teacherId(); if (!id) return; setTemplates((await api(`live_quizzes?teacher_id=eq.${encodeURIComponent(id)}&is_template=eq.true&status=eq.draft&select=id,title,class_code,game_code,status,current_question,is_template&order=created_at.desc`)) || []); } catch (e) { console.error(e); } };
  const loadClasses = async () => { try { const id = teacherId(); if (!id) return; setClasses((await api(`teacher_classes?teacher_id=eq.${encodeURIComponent(id)}&select=id,class_name,school_name,code,teacher_id&order=class_name.asc`)) || []); } catch (e) { console.error(e); setClasses([]); } };
  useEffect(() => { loadTemplates(); loadClasses(); }, []);
  async function createTemplate() { if (!title.trim() || !classCode) { setError('Choose a class and enter a quiz title.'); return; } setLoading(true); setError(''); try { const rows = await api('live_quizzes', { method: 'POST', headers: { ...headers, Prefer: 'return=representation' }, body: JSON.stringify({ title: title.trim(), class_code: classCode, teacher_id: teacherId(), game_code: null, status: 'draft', is_template: true, current_question: -1 }) }); if (!rows?.[0]) throw new Error('Quiz was not created.'); setSelected(rows[0]); setQuestions([]); await loadTemplates(); } catch (e) { setError(e instanceof Error ? e.message : 'Could not create quiz.'); } finally { setLoading(false); } }
  async function openTemplate(t: Quiz) { setSelected(t); setError(''); try { setQuestions((await api(`live_quiz_questions?quiz_id=eq.${t.id}&select=*&order=question_order.asc`)) || []); } catch (e) { setError(e instanceof Error ? e.message : 'Could not load quiz.'); } }
  async function add() { if (!selected || !q.trim() || ![a,b,c,d].every(x => x.trim())) { setError('Fill the question and all four answers.'); return; } try { const rows = await api('live_quiz_questions', { method: 'POST', headers: { ...headers, Prefer: 'return=representation' }, body: JSON.stringify({ quiz_id: selected.id, question_order: questions.length, question: q.trim(), option_a: a.trim(), option_b: b.trim(), option_c: c.trim(), option_d: d.trim(), correct_answer: correct, time_limit_seconds: 30 }) }); if (rows?.length) setQuestions(p => [...p, ...rows]); else setQuestions((await api(`live_quiz_questions?quiz_id=eq.${selected.id}&select=*&order=question_order.asc`)) || []); setQ(''); setA(''); setB(''); setC(''); setD(''); } catch (e) { setError(e instanceof Error ? e.message : 'Could not add question.'); } }
  async function launchTemplate() { if (!selected || !questions.length) { setError('Add at least one question before starting.'); return; } setLoading(true); setError(''); try { const rows = await api('live_quizzes', { method: 'POST', headers: { ...headers, Prefer: 'return=representation' }, body: JSON.stringify({ title: selected.title, class_code: selected.class_code, teacher_id: teacherId(), game_code: code(), status: 'lobby', current_question: -1, is_template: false }) }); if (!rows?.[0]) throw new Error('Could not create the live session.'); const live = rows[0] as Quiz; const cloned = (await api(`live_quiz_questions?quiz_id=eq.${selected.id}&select=question_order,question,option_a,option_b,option_c,option_d,correct_answer,time_limit_seconds&order=question_order.asc`)) || []; for (const item of cloned) await api('live_quiz_questions', { method: 'POST', headers: { ...headers, Prefer: 'return=minimal' }, body: JSON.stringify({ ...item, quiz_id: live.id, time_limit_seconds: 30 }) }); setQuiz(live); setQuestions(cloned.map((x: Q, i: number) => ({ ...x, id: `${live.id}-${i}`, time_limit_seconds: 30 }))); setSelected(null); } catch (e) { setError(e instanceof Error ? e.message : 'Could not start live quiz.'); } finally { setLoading(false); } }
  async function setStatus(status: string, index: number) { if (!quiz) return; try { const rows = await api(`live_quizzes?id=eq.${quiz.id}`, { method: 'PATCH', headers: { ...headers, Prefer: 'return=representation' }, body: JSON.stringify({ status, current_question: index }) }); setQuiz(rows?.[0] || { ...quiz, status, current_question: index }); setShowAnswers(false); } catch (e) { setError(e instanceof Error ? e.message : 'Could not update quiz.'); } }
  async function loadLive() { if (!quiz) return; try { setPlayers((await api(`live_quiz_players?quiz_id=eq.${quiz.id}&select=id,nickname,score,total_response_time_ms,correct_answers&order=correct_answers.desc,total_response_time_ms.asc`)) || []); const x = await api(`live_quizzes?id=eq.${quiz.id}&select=*`); if (x?.[0]) setQuiz(x[0]); } catch (e) { console.error(e); } }
  useEffect(() => { if (!quiz) return; loadLive(); const t = window.setInterval(loadLive, 1000); return () => window.clearInterval(t); }, [quiz?.id]);
  useEffect(() => { if (quiz?.status !== 'question' || !currentQ) return; setRemaining(30); setShowAnswers(false); const started = Date.now(); const timer = window.setInterval(() => { const left = Math.max(0, 30000 - (Date.now() - started)); setRemaining(Math.ceil(left / 1000)); if (left <= 0) { clearInterval(timer); setShowAnswers(true); } }, 100); return () => clearInterval(timer); }, [quiz?.current_question, quiz?.status, currentQ?.id]);
  const copyCode = async () => { if (!quiz?.game_code) return; try { await navigator.clipboard.writeText(quiz.game_code); setCopied(true); window.setTimeout(() => setCopied(false), 1500); } catch {} };
  const rankedPlayers = [...players].sort((x, y) => y.correct_answers - x.correct_answers || x.total_response_time_ms - y.total_response_time_ms);
  const correctCount = currentQ ? players.filter(p => p.correct_answers > current).length : 0;

  if (quiz) return <main className="min-h-screen bg-gradient-to-b from-background via-background to-primary/[0.04] px-4 py-6 sm:px-6 lg:px-8"><div className="mx-auto max-w-7xl space-y-6">
    <div className="flex flex-wrap items-center justify-between gap-4 rounded-3xl border bg-card/90 p-5 shadow-sm backdrop-blur"><div className="flex items-center gap-4"><div className="relative flex size-12 items-center justify-center rounded-2xl bg-primary text-primary-foreground shadow-sm"><span className="absolute inset-0 animate-ping rounded-2xl bg-primary/30" /><Radio className="relative size-6" /></div><div><div className="flex items-center gap-2"><span className="text-xs font-bold uppercase tracking-wider text-primary">Teacher Host Screen</span><span className="rounded-full bg-primary/10 px-2 py-0.5 text-[10px] font-bold text-primary">SCREEN SHARE</span></div><h1 className="text-xl font-bold sm:text-2xl">{quiz.title}</h1></div></div><Button variant="outline" className="rounded-xl" onClick={() => router.push('/dashboard/teacher')}><ArrowLeft className="mr-2 size-4" />Back to Dashboard</Button></div>
    <div className="grid gap-5 lg:grid-cols-[1fr_260px]"><Card className="overflow-hidden rounded-3xl border-0 bg-primary text-primary-foreground shadow-xl"><CardContent className="relative flex flex-col items-center justify-center py-8 text-center sm:py-10"><div className="pointer-events-none absolute inset-0 bg-white/[0.04]" /><p className="relative text-xs font-bold uppercase tracking-[0.3em] opacity-80">Join Code</p><div className="relative mt-2 flex items-center gap-3"><span className="font-black tracking-[0.28em] text-5xl sm:text-6xl">{quiz.game_code}</span><Button size="icon" variant="secondary" className="rounded-xl" onClick={copyCode} aria-label="Copy game code">{copied ? <Check className="size-5" /> : <Copy className="size-5" />}</Button></div><p className="relative mt-3 text-sm opacity-80">Students enter this code to join. Keep this screen shared.</p></CardContent></Card><Card className="rounded-3xl"><CardContent className="flex h-full items-center justify-center gap-4 py-7"><div className="flex size-12 items-center justify-center rounded-2xl bg-muted"><Users className="size-6" /></div><div><p className="text-3xl font-black tabular-nums">{players.length}</p><p className="text-sm text-muted-foreground">Players connected</p></div></CardContent></Card></div>
    {error && <div className="rounded-2xl border border-destructive/30 bg-destructive/5 p-4 text-sm font-medium text-destructive">{error}</div>}
    {quiz.status === 'lobby' && <Card className="rounded-3xl"><CardContent className="py-14 text-center"><div className="mx-auto flex size-16 animate-pulse items-center justify-center rounded-3xl bg-primary/10 text-primary"><MonitorPlay className="size-8" /></div><h2 className="mt-5 text-3xl font-black">Ready when your class is in</h2><p className="mx-auto mt-2 max-w-lg text-muted-foreground">Share this page with your class. Students will see only the four answer shapes on their devices.</p><div className="mx-auto mt-7 max-w-sm rounded-2xl border bg-muted/20 p-4 text-sm"><b>{players.length}</b> player{players.length === 1 ? '' : 's'} connected</div><Button size="lg" className="mt-4 rounded-xl px-7" disabled={!questions.length} onClick={() => setStatus('question', 0)}><Play className="mr-2 size-5" />Start Question 1</Button></CardContent></Card>}
    {quiz.status === 'question' && currentQ && <div className="grid gap-6 lg:grid-cols-[minmax(0,2fr)_340px]"><Card className="overflow-hidden rounded-3xl"><CardHeader className="border-b bg-muted/30 p-6 sm:p-8"><div className="flex items-center justify-between gap-3"><span className="rounded-full bg-primary/10 px-3 py-1 text-xs font-bold text-primary">QUESTION {current + 1} / {questions.length}</span><span className={`inline-flex min-w-20 items-center justify-center gap-2 rounded-full px-4 py-2 text-xl font-black tabular-nums transition-all ${remaining <= 5 ? 'animate-pulse bg-destructive/10 text-destructive' : 'bg-primary/10 text-primary'}`}><Clock className="size-5" />{remaining}s</span></div><div className="mt-5 h-2 overflow-hidden rounded-full bg-muted"><div className={`h-full rounded-full transition-all duration-100 ${remaining <= 5 ? 'bg-destructive' : 'bg-primary'}`} style={{ width: `${(remaining / 30) * 100}%` }} /></div><CardTitle className="mt-7 text-center text-3xl leading-tight sm:text-4xl">{currentQ.question}</CardTitle><p className="mt-3 text-center text-xs font-medium uppercase tracking-wider text-muted-foreground">Students answer on their own devices</p></CardHeader><CardContent className="p-5 sm:p-7"><div className="grid grid-cols-2 gap-3 sm:gap-4">{answerColors.map(x => <div key={x.letter} className={`min-h-32 rounded-2xl p-5 text-white shadow-sm transition-all duration-200 hover:-translate-y-0.5 hover:shadow-lg sm:min-h-36 sm:p-6 ${x.className}`}><div className="flex items-center justify-between"><span className="text-4xl font-black">{x.label}</span><span className="rounded-lg bg-black/10 px-2 py-1 text-xs font-black">{x.letter}</span></div><p className="mt-4 text-lg font-bold sm:text-xl">{currentQ[`option_${x.letter.toLowerCase()}` as 'option_a'|'option_b'|'option_c'|'option_d']}</p></div>)}</div>
      {showAnswers && <div className="mt-5 rounded-2xl border border-primary/20 bg-primary/5 p-4"><div className="flex flex-wrap items-center justify-between gap-2"><div><p className="text-xs font-bold uppercase tracking-wider text-muted-foreground">Answer reveal</p><p className="text-lg font-black">Correct answer: {currentQ.correct_answer}</p></div><div className="rounded-xl bg-background px-3 py-2 text-sm font-bold">{correctCount} players currently have this many correct: {current + 1}</div></div><div className="mt-4 grid grid-cols-2 gap-2 sm:grid-cols-4">{answerColors.map(x => <div key={x.letter} className={`rounded-xl border p-3 text-center ${x.letter === currentQ.correct_answer ? 'border-primary bg-primary/10 font-black' : 'bg-background/60'}`}><div className="text-2xl">{x.label}</div><div className="text-xs font-bold">{x.letter}</div></div>)}</div></div>}
      <div className="mt-5 flex flex-wrap items-center justify-between gap-3"><span className="text-xs font-medium text-muted-foreground">{showAnswers ? 'Answers revealed — review the leaderboard before continuing.' : 'Answers are hidden from students until the timer ends.'}</span><Button className="rounded-xl" onClick={() => current + 1 < questions.length ? setStatus('question', current + 1) : setStatus('finished', current)}>{current + 1 < questions.length ? 'Next Question' : 'Finish Quiz'}<ChevronRight className="ml-1 size-4" /></Button></div></CardContent></Card><Card className="rounded-3xl"><CardHeader><CardTitle className="flex items-center gap-2"><Trophy className="size-5 text-primary" />Live Leaderboard</CardTitle><p className="text-xs text-muted-foreground">Correct answers first · fastest total time breaks ties</p></CardHeader><CardContent className="space-y-2">{rankedPlayers.length ? rankedPlayers.map((p, i) => <div key={p.id} className={`flex items-center gap-3 rounded-xl border p-3 transition-all duration-300 ${i === 0 ? 'border-primary/30 bg-primary/5' : ''}`}><span className="w-7 text-center text-sm font-black">{i + 1}</span><div className="min-w-0 flex-1"><p className="truncate font-bold">{p.nickname}</p><p className="text-xs text-muted-foreground">{p.correct_answers} correct · {(p.total_response_time_ms / 1000).toFixed(1)}s</p></div><span className="font-black tabular-nums">{p.score}</span></div>) : <p className="py-8 text-center text-sm text-muted-foreground">Waiting for answers...</p>}</CardContent></Card></div>}
    {quiz.status === 'finished' && <Card className="overflow-hidden rounded-3xl"><CardContent className="py-12 text-center sm:py-16"><Sparkles className="mx-auto size-10 text-primary" /><h2 className="mt-4 text-4xl font-black">Quiz Complete!</h2><p className="mt-2 text-muted-foreground">Final rankings are based on correct answers, then total response time.</p><div className="mx-auto mt-8 grid max-w-3xl gap-3 sm:grid-cols-3">{rankedPlayers.slice(0, 3).map((p, i) => <div key={p.id} className={`rounded-3xl border p-6 shadow-sm ${i === 0 ? 'order-first border-primary/40 bg-primary/5 sm:-translate-y-3' : ''}`}><div className="text-4xl">{['🥇','🥈','🥉'][i]}</div><p className="mt-3 truncate text-xl font-black">{p.nickname}</p><p className="mt-1 text-sm text-muted-foreground">{p.correct_answers} correct</p><p className="mt-2 text-2xl font-black">{p.score}</p></div>)}</div><div className="mx-auto mt-8 max-w-2xl space-y-2">{rankedPlayers.slice(3).map((p, i) => <div key={p.id} className="flex items-center gap-3 rounded-xl border p-3 text-left"><span className="w-8 font-black">{i + 4}</span><span className="flex-1 font-bold">{p.nickname}</span><span className="text-sm text-muted-foreground">{p.correct_answers} correct</span><span className="font-black">{p.score}</span></div>)}</div><Button className="mt-8 rounded-xl" onClick={() => router.push('/dashboard/teacher')}><ArrowLeft className="mr-2 size-4" />Back to Dashboard</Button></CardContent></Card>}
  </div></main>;

  return <main className="min-h-screen bg-gradient-to-b from-background to-primary/[0.04] px-4 py-6"><div className="mx-auto max-w-6xl space-y-6"><div className="flex items-center justify-between"><div><Button variant="outline" onClick={() => router.push('/dashboard/teacher')}><ArrowLeft className="mr-2 size-4" />Back to Dashboard</Button><h1 className="mt-5 text-4xl font-black">Live Quiz</h1><p className="mt-1 text-muted-foreground">Create a quiz template, then use it for a live class game.</p></div></div>{error && <div className="rounded-2xl border border-destructive/30 bg-destructive/5 p-4 text-sm text-destructive">{error}</div>}<div className="grid gap-6 lg:grid-cols-[1.1fr_1fr]">{!selected ? <Card className="rounded-3xl"><CardHeader><CardTitle>Create Quiz Template</CardTitle></CardHeader><CardContent className="space-y-4"><Input value={title} onChange={e=>setTitle(e.target.value)} placeholder="Quiz title"/><select className="h-10 w-full rounded-xl border bg-background px-3" value={classCode} onChange={e=>setClassCode(e.target.value)}><option value="">Select a class</option>{classes.map(c=><option key={c.code} value={c.code}>{c.class_name} — {c.code}</option>)}</select><Button className="w-full rounded-xl" onClick={createTemplate} disabled={loading}><Plus className="mr-2 size-4"/>Create Template</Button></CardContent></Card> : <Card className="rounded-3xl"><CardHeader><CardTitle>{selected.title}</CardTitle></CardHeader><CardContent className="space-y-4"><div className="rounded-2xl border bg-muted/20 p-4 text-sm"><b>{questions.length}</b> question{questions.length===1?'':'s'} · 30 seconds each</div><Input value={q} onChange={e=>setQ(e.target.value)} placeholder="Question"/><div className="grid gap-2 sm:grid-cols-2"><Input value={a} onChange={e=>setA(e.target.value)} placeholder="Answer A"/><Input value={b} onChange={e=>setB(e.target.value)} placeholder="Answer B"/><Input value={c} onChange={e=>setC(e.target.value)} placeholder="Answer C"/><Input value={d} onChange={e=>setD(e.target.value)} placeholder="Answer D"/></div><select className="h-10 w-full rounded-xl border bg-background px-3" value={correct} onChange={e=>setCorrect(e.target.value)}><option value="A">Correct: A</option><option value="B">Correct: B</option><option value="C">Correct: C</option><option value="D">Correct: D</option></select><Button className="w-full rounded-xl" onClick={add}><Plus className="mr-2 size-4"/>Add Question</Button><Button variant="outline" className="w-full rounded-xl" onClick={launchTemplate} disabled={loading || !questions.length}><Play className="mr-2 size-4"/>Start This Quiz Live</Button></CardContent></Card>}{!selected && <Card className="rounded-3xl"><CardHeader><CardTitle>Saved Quiz Templates</CardTitle></CardHeader><CardContent className="space-y-2">{templates.length ? templates.map(t=><button key={t.id} onClick={()=>openTemplate(t)} className="w-full rounded-2xl border p-4 text-left transition hover:-translate-y-0.5 hover:bg-muted/40"><div className="flex items-center justify-between"><span className="font-bold">{t.title}</span><ChevronRight className="size-4"/></div><span className="text-xs text-muted-foreground">Class: {t.class_code}</span></button>) : <p className="py-8 text-center text-sm text-muted-foreground">No saved templates yet.</p>}</CardContent></Card>}</div></div></main>;
}
